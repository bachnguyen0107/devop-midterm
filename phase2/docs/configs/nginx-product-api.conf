# Nginx Reverse Proxy Configuration
# File location: /etc/nginx/sites-available/product-api
# Used for: Forwarding HTTP/HTTPS traffic to Node.js app on port 3000
# SSL: Let's Encrypt certificate for devop-midterm2026.online

# HTTPS (Primary server block)
server {
    server_name devop-midterm2026.online;

    # Logs
    access_log /var/log/nginx/product-api-access.log;
    error_log /var/log/nginx/product-api-error.log;

    # Reverse proxy to Node.js app on port 3000
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;

        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_cache_bypass $http_upgrade;
    }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/devop-midterm2026.online/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/devop-midterm2026.online/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}

# HTTP - redirect to HTTPS
server {
    if ($host = devop-midterm2026.online) {
        return 301 https://$host$request_uri;
    } # managed by Certbot

    listen 80;
    server_name devop-midterm2026.online;
    return 404; # managed by Certbot
}
